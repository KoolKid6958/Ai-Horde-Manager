import { Textf } from "blocks.slint";

export component Main_Window inherits Window {
    Textf {
        text: "Ai horde manager!";
    }
    title: "Ai Horde Manager";
    preferred-height: 700px;
    preferred-width: 450px;
    min-height: 300px;
    min-width: 450px;
    background: #1f1f1f;
}
